#include <stdio.h>

int main(){
    //declaramos variables
    int numeroEntero;
    double numerosDecimales;
    float numerosDecimales2;
    char caracter; //?

    //introduce por pantalla un numero, pide un numero
    printf("Introduce un numero: ");
    scanf("%d", &numeroEntero); //introducir un numero
    printf("%d", numeroEntero); //mostrarlo

    //empezamos el programa
    //1. condiciones --> IF Y SWITCH
    if(codicion--> num>5){
        //si lo cumple voy a poner aqui las instrucciones
        printf("lo cumple");
    }else{
        //si no lo cumple
        printf("no lo cumple");
    }



}